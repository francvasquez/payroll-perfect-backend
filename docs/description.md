## Console main catchup post db write setup
- Ensure Lambda prod is updated to dev in runtime in console and check layers, also check env variables. 
- Add Lambda to VPC
- Update RDS Securityu Groups
- VPC manager ensure S3 access to VPC
